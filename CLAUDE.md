# compass-aa-tool — 開発ガイド

『#コンパス』のゲーム内チャットで崩れない AA（アスキーアート）を作る Web ツール。
複数行 AA を貼り付ける／ドット打ちで描くと、自動改行だけで形が再現されるよう
スペーサーを自動挿入し、コピー用の 1 行テキストを出力する。
GitHub Pages で公開（完全静的・サーバなし）。

## 最初に読むもの

| ドキュメント | 内容 |
|---|---|
| `docs/notes/calibration-plan.md` | **最重要**。実機校正の全ラウンド結果と確定モデル。数値の根拠は全てここ |
| `docs/notes/dot-editor-requirements.md` | ドット打ちエディタの確定要件 |
| `docs/spec.md` | 当初仕様（ユーザー提供）。**数値は校正前の推定値**で現在は上書き済み |
| `materials/README.md` | 実機検証データ（スクショ＋入力ペア）の台帳 |

## 公開先

`https://cccurious.com/compass-aa-tool/`（GitHub Pages ＋ カスタムドメイン）。
BM チェッカー（`/draw-need-bm/`）と同じドメインのサブパスに並ぶ。

- Vite の `base` は `'./'`（相対）。サブパス配信でそのまま動く。
- **リンクは必ず末尾スラッシュ付きで貼ること**（`/compass-aa-tool/`）。
  スラッシュ無しだと相対パスの起点が 1 つ上になる。GitHub Pages が
  リダイレクトで補ってくれるが、依存しない方が安全。
- OGP・canonical を入れるときの URL もこれ。

## 不可侵の制約

1. **GitHub Pages 配信のみ**: サーバサイド・外部 API・継続課金要素を持ち込まない。
2. **既存 BM チェッカー（draw-need-bm-react）のコードは触らない**。UI の見た目・
   構成パターンを参照するのみ（読み取りは可）。
3. UI はスマホ優先（ゲームと同じ端末でコピペする利用が主）。

## 確定済みの実機モデル（2026-07-25 校正完了・CALIBRATED=true）

**この 4 つがツールの土台。変更には実機プローブによる反証が必要。**

1. **折り返し**: 半角スペースに当たると「次の半角スペースまでの語（全角スペース込み）」を
   先読みし、行の残り幅に入らなければその場で改行（前倒し）。それ以外は文字単位で折り、
   後方への巻き戻しはしない。行頭禁則なし。しきい値 L ∈ [20.476, 20.548)。
2. **空白**: 折り返し点の半角スペースは行末・行頭とも消える（幅 0.34 は行中のみ）。
   全角スペースは消えず次行頭へキャリー＝字下げになる。
3. **文字幅の 3 層**: ①かな・漢字・全角記号・全角スペース = 固定 1.0
   ②半角（ASCII・半角カナ）= BIZ UDPGothic 幅 ③アトラス収録漏れ = 全角 1.0／半角 0.5 の
   フォールバック。実測確定値は `DEVICE_OVERRIDES` が最優先（´=1.0, _=0.5, 記号 8 種）。
4. **1 メッセージ上限は二重**（2026-07-26 v5・実機 23 本で確定）。超過分は入力欄の
   確定時に末尾から切り捨て。判定は `core/limit.ts` の messageCost()。
   - **上限 A（長さ 196）**: 全角グリフ 1・半角/全角スペース/改行しないスペース 0.75・
     **改行を発生させた半角スペースだけ 6.75**（＝改行 1 回で全角 6 文字ぶん）
   - **上限 B（幅換算 184）**: 全角 1・半角（スペース含む）0.5
   「改行したスペースだけ高い」が肝。同じ 159 字でも 2 語が 1 行に収まる構成は無傷で、
   改行を伴う構成は切断される。旧説（512 バイト・units 一律 16・全角直後 16）は全て反証済み。

### 幅が未確認の文字が出たときの手順（ルーチン化済み）

UI が「幅が未確認の文字」と警告したら、`あ`×N ＋ 対象文字を並べたプローブ 1 本を
ユーザーに送信・スクショ依頼 → 折り返し位置から幅を確定 → `FALLBACK_VERIFIED_RANGES`
（範囲ごと）か `DEVICE_OVERRIDES`（1 文字）へ追記。実績: ⌒ ´ _ 罫線 ブロック 幾何学図形。

### 再校正のサイン

幅テーブルはゲーム固有の TextMeshPro 設定（元フォント＋アトラス収録リスト＋
フォールバック先）の産物であり、公開フォント単体とは一致しない。**ゲーム更新後に
出力が一斉にずれ始めたら再校正**（calibration-plan.md のフェーズ A から回し直す）。

## 設計ルール

- `src/core/` は React を import しない（純関数・vitest で直接テスト）。
- 幅・しきい値の数値は `src/core/metrics.ts` に集約。他所へ直書きしない。
- 正規表現に全角域を書くときは**必ず `\u` エスケープ**（字形が似た別コードポイントの
  混入を目視で検出できないため。実績: 豈 のつもりで U+8C48 を書き私用領域まで全角扱い）。
- 情報の置き場所の四原則: コードには How ／ テストには What ／
  コミットログには Why ／ コードコメントには Why not（実測・制約ベースで書く）。
- 1 コミット 1 意図。テストと実装は同一コミット。

## 検証の掟（実機データの読み方）

- **コピーバック**（送信済みメッセージを実機でコピーして回収）は文字の同一性の
  オラクル。ただし**表示の姿のオラクルではない**（`. / :` はコピー時のみ全角化される。
  表示は半角のまま）。見た目は必ずスクショで確認する。
- 幅の測定は「折り返し位置」か「同一文字連続のピッチ」を使う。単発グリフの
  インク幅は LSB の影響を受けるので使わない。
- 実装より先に測る（`.claude/skills/diagnosis-first`）。結論と数字は
  calibration-plan.md へ必ず永続化する（後続セッションの再診断を防ぐ）。

## 体制

Opus 5 がメイン（2026-07-25 にユーザーがモデル切替）。定型実装・文言整備・
スクリプト実行系はサブエージェントへ切り出してよい（`write-plans` で手順書化してから渡す）。
実機データを見て即断即実装する密結合ループは単独実行のままでよい。
**同じ問題で 2 回失敗 / 確定モデルの数値を反証なしに変えたくなった → 止めてユーザーへ報告。**

## コマンド

```
npm run dev      # 開発サーバ（Browser ペインの preview_start "compass-aa-tool" 経由）
npm test         # vitest（metrics / convert / store）
npm run lint     # ESLint（CI でも build 前に走る。例外 2 件の理由は eslint.config.js 参照）
npm run format   # Prettier（TS/TSX のみ。style.css は対象外＝ .prettierignore に理由）
npm run build    # tsc + vite build（main へ push で Pages へ自動デプロイ）
```

## 現在地と残タスク

- 公開済み（GitHub Pages・main へ push で自動デプロイ）。GA4 は G-VF12E12C4Q。
- 実装済み: ドット打ちエディタ（20 列グリッド・7 カテゴリのパレット・最近使った文字の保存）/
  AA 貼り付け変換 / 幅テーブル駆動 WYSIWYG プレビュー / 1 文字ブレーク最適化 /
  使い方ビュー / ヘルプ / 応援バナー / `?view=` URL パラメータ。
- 残タスク:
  - OGP・Twitter カード・canonical・JSON-LD（URL は上記の公開先）
  - GA4 のカスタムディメンション登録（`source` 等。遡及しないので早めに）
  - スマホでマスが小さい件の対策（拡大トグル案）
  - ポータル（cccurious-portal）側の導線調整
