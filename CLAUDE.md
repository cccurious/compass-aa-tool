# compass-aa-tool — 開発ガイド

『#コンパス』のゲーム内チャットで崩れない AA（アスキーアート）を作る Web ツール。
複数行 AA を貼り付けると、自動改行だけで形が再現されるようスペーサーを自動挿入し、
コピー用の 1 行テキストを出力する。GitHub Pages で公開（完全静的・サーバなし）。

## 最初に読むもの

| ドキュメント | 内容 |
|---|---|
| `docs/spec.md` | 表示法則の仕様（1行=全角20.5・キャリーオーバー・Word Wrap 地雷） |
| `docs/notes/` | 校正結果・診断の数字と結論（セッションを跨ぐ知見はここへ） |
| `materials/README.md` | 実機検証データ（iPhone スクショ＋入力ペア）の置き場と台帳 |

## 不可侵の制約

1. **GitHub Pages 配信のみ**: サーバサイド・外部 API・継続課金要素を持ち込まない。
2. **既存 BM チェッカー（draw-need-bm-react）のコードは触らない**。UI の見た目・
   構成パターンを参照するのみ（読み取りは可）。
3. UI はスマホ優先（ゲームと同じ端末でコピペする利用が主）。

## 凍結（校正まで触らない・積み増さない）

- `src/core/metrics.ts` の幅数値（LINE_LIMIT=20.5・文字幅・スペーサー幅）は
  **仕様書由来の未校正仮値**。実機データでの校正が済むまで、この値を前提にした
  精密機能（狭幅記号スペーサー・キャリーオーバー制御・斜め線調整）を実装しない。
- 校正は diagnosis-first Skill の手順で行い、結果（数字・判定・根拠スクショの台帳）を
  `docs/notes/` に固定してから `CALIBRATED = true` にする。
- **未検証事項**（実機確認が必要。ユーザーに採取を依頼できる）:
  - 半角/全角スペースの実機幅・行末トリムの有無
  - 狭幅記号（`,` `;` `\` 等）のチャット入力可否と実機幅
  - 1 メッセージの最大文字数
  - Android との差異（現状の検証データは iPhone のみ）

## 設計ルール

- `src/core/` は React を import しない（純関数・vitest で直接テスト）。
- 幅・しきい値の数値は `src/core/metrics.ts` に集約。他所へ直書きしない。
- 情報の置き場所の四原則: コードには How ／ テストには What ／
  コミットログには Why ／ コードコメントには Why not（実測・制約ベースで書く）。
- 1 コミット 1 意図。テストと実装は同一コミット。

## 作業の進め方

- 定型外の設計判断・複数コミットの計画は `.claude/skills/write-plans` に従い
  手順書化する。実行時は手順書に書かれていない改善を足さない。
- ヒューリスティック（幅推定・校正・最適化）は `.claude/skills/diagnosis-first` に
  従い、実装の前に正解既知のデータで測る。
- 同じ問題で 2 回失敗 / 凍結値を触りたくなった → 止めてユーザーへ報告。

## コマンド

```
npm run dev      # 開発サーバ
npm test         # vitest（core のユニットテスト）
npm run build    # tsc + vite build（main へ push で Pages へ自動デプロイ）
```
