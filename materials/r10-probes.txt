R10 プローブ集（各行を丸ごとコピーして 1 メッセージで送信 → スクショ）

R10-1 richText タグ <br>（スマホ手打ち可）:
あ<br>い

R10-2 タブ U+0009（あ と い の間にタブ）:
あ	い

R10-3 垂直タブ U+000B:
あい

R10-4 LINE SEPARATOR U+2028:
あ い

R10-5 NEL U+0085:
あい

判定: 「あ / い」が 2 行になれば改行の穴あり（大収穫）。
1 行のままなら幅だけ確認（タブは幅測定値としても有用）。
文字が消えた場合はフィルタで除去（不採用）。